'use client';
// Admin page to display debug information and manage the application.

import React from 'react';

export default function AdminPage() {
    return (
        <div>
            <p>Admin page is temporarily disabled.</p>
        </div>
    );
}