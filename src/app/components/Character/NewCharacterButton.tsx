import { Button } from "@radix-ui/themes";

export const NewCharacterButton = () => {
    return (
        <>
        <Button>
            Create a new character
        </Button>
        </>
    );
};
